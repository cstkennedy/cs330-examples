#! /bin/sh

# Test Tool::read and Tool::display
# formatting must match

cat <<EOF > test002.cpp

#include <iostream>
#include <iomanip>
#include <sstream>

#include "Tool.h"

using std::cout;
using std::istringstream;

int main(){
    Item *tool = new Tool(); 

    istringstream ins( "Shovel Gold 20 3 Unbreaking 2" );

    tool->read( ins );    
    tool->display( cout );  

    return 0;
}
EOF

g++ -c test002.cpp 
g++ -o test002 test002.o Tool.o Item.o

./test002